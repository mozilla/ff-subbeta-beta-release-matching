## Importing packages
library(kableExtra)      # help you build common complex tables and manipulate table styles
library(tidyverse)       # for general data wrangling (includes readr and dplyr)
library(ggplot2)         # to draw statistical plots
library(Amelia)          # to visualize missing data
library(plotly)          # to construct interactive 3d plots
library(DataExplorer)    # automated data exploration
library(corrplot)        # to plot nice correlation matrix
library(caret)           # includes several functions to pre-process
library(scales)          # to determining breaks and labels for axes and legends
library(vtreat)          # to variable preparation for ML
library(semTools)        # useful tools for structural equation modeling
library(Boruta)          # to use relevant feature selection wrapper algorithm
load("~/GitHub/ff-beta-release-matching/poc/EDA/data_milestone2_df_train_validate_20191021.RData")
# loading the training dataset
load("~/GitHub/ff-beta-release-matching/poc/EDA/data_milestone2_df_train_validate_20191021.RData")
# loading the validation dataset
load("~/GitHub/ff-beta-release-matching/poc/EDA/data_milestone2_df_train_validate_20191025.RData")
# loading the training dataset
load("~/GitHub/ff-beta-release-matching/poc/EDA/data_milestone2_df_train_validate_20191021.RData")
# view train dataframe
kable(head(df_train_f)) %>%
kable_styling(bootstrap_options = c("striped", "hover", "condensed"), full_width = F) %>%
scroll_box(width = "100%")
kable(introduce(df_train_f)) %>%
kable_styling(bootstrap_options = c("striped", "hover", "condensed"), full_width = F) %>%
scroll_box(width = "100%")
plot_intro(df, ggtheme = theme_minimal())
plot_intro(df_train_f, ggtheme = theme_minimal())
glimpse(df_train_f)
plot_density(df_train_f, nrow = 65L, ncol = 6L)
plot_str(df_train_f)
plot_str(df_train_f)
plot_histogram(df_train_f, nrow = 65L, ncol = 6L)
View(diamonds)
## Frequency distribution of all discrete variables
plot_bar(df_train_f)
plot_correlation(df_train_f, type = 'continuous','Review.Date')
## Importing packages
library(kableExtra)      # help you build common complex tables and manipulate table styles
library(tidyverse)       # for general data wrangling (includes readr and dplyr)
library(ggplot2)         # to draw statistical plots
library(Amelia)          # to visualize missing data
library(plotly)          # to construct interactive 3d plots
library(DataExplorer)    # automated data exploration
library(corrplot)        # to plot nice correlation matrix
library(caret)           # includes several functions to pre-process
library(scales)          # to determining breaks and labels for axes and legends
library(vtreat)          # to variable preparation for ML
library(semTools)        # useful tools for structural equation modeling
library(Boruta)          # to use relevant feature selection wrapper algorithm
# view train dataframe
kable(head(df_train_f)) %>%
kable_styling(bootstrap_options = c("striped", "hover", "condensed"), full_width = F) %>%
scroll_box(width = "100%")
kable(introduce(df_train_f)) %>%
kable_styling(bootstrap_options = c("striped", "hover", "condensed"), full_width = F) %>%
scroll_box(width = "100%")
plot_intro(df_train_f, ggtheme = theme_minimal())
plot_str(df_train_f)
plot_correlation(df_train_f, type = 'continuous','Review.Date')
plot_correlation(df_train_f, type = 'continuous')
plot_correlation(df_train_f, type = 'continuous')
cat <- unlist(lapply(df_train_f, is.numeric))
correlations <- cor(df_train_f[, cat])
# correlations
row_indic <- apply(correlations, 1, function(x) sum(x > 0.3 | x < -0.3) > 1)
correlations <- correlations[row_indic,row_indic]
corrplot(correlations, method = "color", order = "FPC", number.cex = .55,
addCoef.col = "black", tl.srt = 45, # Add coefficient of correlation
tl.col = "black")
cat <- unlist(lapply(df_train_f, is.numeric))
correlations <- cor(df_train_f[, cat])
# correlations
#row_indic <- apply(correlations, 1, function(x) sum(x > 0.3 | x < -0.3) > 1)
#correlations <- correlations[row_indic,row_indic]
corrplot(correlations, method = "color", order = "FPC", number.cex = .55,
addCoef.col = "black", tl.srt = 45, # Add coefficient of correlation
tl.col = "black")
## Importing packages
library(kableExtra)      # help you build common complex tables and manipulate table styles
library(tidyverse)       # for general data wrangling (includes readr and dplyr)
library(ggplot2)         # to draw statistical plots
library(Amelia)          # to visualize missing data
library(plotly)          # to construct interactive 3d plots
library(DataExplorer)    # automated data exploration
library(corrplot)        # to plot nice correlation matrix
library(caret)           # includes several functions to pre-process
library(scales)          # to determining breaks and labels for axes and legends
library(vtreat)          # to variable preparation for ML
library(semTools)        # useful tools for structural equation modeling
library(Boruta)          # to use relevant feature selection wrapper algorithm
library(skimr)
install.packages('skimr')
## Importing packages
library(kableExtra)      # help you build common complex tables and manipulate table styles
library(tidyverse)       # for general data wrangling (includes readr and dplyr)
library(ggplot2)         # to draw statistical plots
library(Amelia)          # to visualize missing data
library(plotly)          # to construct interactive 3d plots
library(DataExplorer)    # automated data exploration
library(corrplot)        # to plot nice correlation matrix
library(caret)           # includes several functions to pre-process
library(scales)          # to determining breaks and labels for axes and legends
library(vtreat)          # to variable preparation for ML
library(semTools)        # useful tools for structural equation modeling
library(Boruta)          # to use relevant feature selection wrapper algorithm
library(skimr)
plot_str(df_train_f)
skim(df_train_f)
str(df_train_f)
df_status(df_train_f)
## Importing packages
library(kableExtra)      # help you build common complex tables and manipulate table styles
library(tidyverse)       # for general data wrangling (includes readr and dplyr)
library(ggplot2)         # to draw statistical plots
library(Amelia)          # to visualize missing data
library(plotly)          # to construct interactive 3d plots
library(DataExplorer)    # automated data exploration
library(corrplot)        # to plot nice correlation matrix
library(caret)           # includes several functions to pre-process
library(scales)          # to determining breaks and labels for axes and legends
library(vtreat)          # to variable preparation for ML
library(semTools)        # useful tools for structural equation modeling
library(Boruta)          # to use relevant feature selection wrapper algorithm
library(skimr)
library(funModeling)
## Importing packages
library(kableExtra)      # help you build common complex tables and manipulate table styles
library(tidyverse)       # for general data wrangling (includes readr and dplyr)
library(ggplot2)         # to draw statistical plots
library(Amelia)          # to visualize missing data
library(plotly)          # to construct interactive 3d plots
library(DataExplorer)    # automated data exploration
library(corrplot)        # to plot nice correlation matrix
library(caret)           # includes several functions to pre-process
library(scales)          # to determining breaks and labels for axes and legends
library(vtreat)          # to variable preparation for ML
library(semTools)        # useful tools for structural equation modeling
library(Boruta)          # to use relevant feature selection wrapper algorithm
library(skimr)
library(funModeling)
install.packages("funModeling")
## Loading the needed libraries
library(kableExtra)      # help you build common complex tables and manipulate table styles
library(tidyverse)       # for general data wrangling (includes readr and dplyr)
library(ggplot2)         # to draw statistical plots
library(Amelia)          # to visualize missing data
library(plotly)          # to construct interactive 3d plots
library(DataExplorer)    # automated data exploration
library(corrplot)        # to plot nice correlation matrix
library(caret)           # includes several functions to pre-process
library(scales)          # to determining breaks and labels for axes and legends
library(vtreat)          # to variable preparation for ML
library(semTools)        # useful tools for structural equation modeling
library(Boruta)          # to use relevant feature selection wrapper algorithm
library(skimr)
library(funModeling)
library(Hmisc)
## View train dataframe
kable(head(df_train_f)) %>%
kable_styling(bootstrap_options = c("striped", "hover", "condensed"), full_width = F) %>%
scroll_box(width = "100%")
df_status(df_train_f)
freq(df_train_f$label)
describe(df_train_f$label)
df_release <- df_train_f[which(df_train_f$label == 'release'), ]
df_beta <- df_train_f[which(df_train_f$label == 'beta'), ]
freq(df_release)
View(df_release)
df_release <- df_train_f[which(df_train_f$label == 'release'), ]
df_beta <- df_train_f[which(df_train_f$label == 'beta'), ]
freq(df_train_f$label)
df_status(df_train_f)
## Loading the needed libraries
library(kableExtra)      # help you build common complex tables and manipulate table styles
library(tidyverse)       # for general data wrangling (includes readr and dplyr)
library(ggplot2)         # to draw statistical plots
library(plotly)          # to construct interactive 3d plots
library(DataExplorer)    # automated data exploration
library(corrplot)        # to plot nice correlation matrix
library(caret)           # includes several functions to pre-process
library(scales)          # to determining breaks and labels for axes and legends
library(skimr)
library(funModeling)
library(Hmisc)
## Loading the training dataset
load("~/GitHub/ff-beta-release-matching/poc/EDA/data_milestone2_df_train_validate_20191021.RData")
## Loading the needed libraries
library(kableExtra)      # help you build common complex tables and manipulate table styles
library(tidyverse)       # for general data wrangling (includes readr and dplyr)
library(ggplot2)         # to draw statistical plots
library(plotly)          # to construct interactive 3d plots
library(DataExplorer)    # automated data exploration
library(corrplot)        # to plot nice correlation matrix
library(caret)           # includes several functions to pre-process
library(scales)          # to determining breaks and labels for axes and legends
library(skimr)
library(funModeling)
library(Hmisc)
## View train dataframe
kable(head(df_train_f)) %>%
kable_styling(bootstrap_options = c("striped", "hover", "condensed"), full_width = F) %>%
scroll_box(width = "100%")
kable(introduce(df_train_f)) %>%
kable_styling(bootstrap_options = c("striped", "hover", "condensed"), full_width = F) %>%
scroll_box(width = "100%")
plot_intro(df_train_f, ggtheme = theme_minimal())
str(df_train_f)
kable(df_status(df_train_f)) %>%
kable_styling(bootstrap_options = c("striped", "hover", "condensed"), full_width = F) %>%
scroll_box(width = "100%")
df_release <- df_train_f[which(df_train_f$label == 'release'), ]
df_beta <- df_train_f[which(df_train_f$label == 'beta'), ]
freq(df_train_f$label)
## Frequency distribution of all discrete variables
plot_bar(df_train_f)
## Frequency distribution for release dataframe
plot_bar(df_release)
## Frequency distribution beta dataframe
plot_bar(df_beta)
## View histogram of all continuous variables
plot_histogram(df_train_f, nrow = 65L, ncol = 6L)
## View histogram of all continuous variables
plot_histogram(df_release, nrow = 65L, ncol = 6L)
## View histogram of all continuous variables
plot_histogram(df_beta, nrow = 65L, ncol = 6L)
## View histogram of all continuous variables
plot_histogram(df_release)
## View histogram of all continuous variables
plot_histogram(df_train_f)
## View histogram of all continuous variables
plot_histogram(df_beta)
